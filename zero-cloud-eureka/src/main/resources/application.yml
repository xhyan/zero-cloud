server:
  port: 9001

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: 127.0.0.1
  client:
    fetch-registry: true
    register-with-eureka: false
    serviceUrl.defaultZone: http://127.0.0.1:${server.port}/eureka/
