#指定服务端口
server:
  port: 2002
#指定微服务名称
spring:
  application:
    name: compute-service
#指定服务中心位置
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl.defaultZone: http://localhost:9001/eureka/

